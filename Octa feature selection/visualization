from IPython.display import Image, display

print("--- Displaying Pre-Saved Publication Figures ---\n")

# List of the files generated by your pipeline
saved_images = [
    "publication_roc_curve.png",
    "publication_confusion_matrix.png",
    "publication_lasso_coef_plot.png",
    "publication_final_coef_plot.png"
]

for img_path in saved_images:
    try:
        print(f"Displaying: {img_path}")
        display(Image(filename=img_path))
        print("-" * 50)
    except FileNotFoundError:
        print(f"[Warning] Could not find '{img_path}'. Make sure your main pipeline script has run and saved it in this directory.\n")
